import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.catastroApp.catastroApp',
  appName: 'app-catastro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
